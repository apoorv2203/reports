import { useMemo, useState } from 'react';
import { useT } from '@/providers/I18nProvider';
import { AppButton } from '@/components/app/AppButton';
import { AppInput } from '@/components/app/AppInput';
import { ArrowLeft, ArrowRight, Check, Database, X } from 'lucide-react';
import { mockSchema, type ParameterInputType, type ReportParameter } from '@/data/reportTemplates';

export function PublishReportDialog({ onClose, onPublished }: { onClose: () => void; onPublished: (parameters: ReportParameter[]) => void }) {
  const t = useT();
  const [step, setStep] = useState<1 | 2>(1);
  const [selected, setSelected] = useState<string[]>([]);
  const [selectorOpen, setSelectorOpen] = useState(false);
  const [parameters, setParameters] = useState<ReportParameter[]>([]);
  const selectedColumns = useMemo(() => mockSchema.flatMap((table) => table.columns.filter((column) => selected.includes(`${table.name}.${column.name}`)).map((column) => ({ table, column }))), [selected]);

  function configure() {
    setParameters(selectedColumns.map(({ table, column }) => ({ id: `${table.name}-${column.name}`, table: table.name, column: column.name, label: column.label, type: column.suggestedType, required: false, options: column.options, defaultValue: column.suggestedType === 'multi-select' || column.suggestedType === 'date-range' ? [] : '' })));
    setStep(2);
  }

  return <div className="fixed inset-0 z-[70] flex items-center justify-center bg-navy-900/55 p-4" role="dialog" aria-modal="true" aria-labelledby="publish-title">
    <div className="flex max-h-[88vh] w-full max-w-3xl flex-col overflow-hidden rounded-[20px] bg-white shadow-2xl">
      <header className="flex items-center justify-between border-b border-surface-200 px-6 py-4"><div><p className="text-[11px] font-bold uppercase tracking-[0.12em] text-mint-700">{t('reports.stepOfTwo', { step })}</p><h2 id="publish-title" className="mt-1 font-display text-[20px] font-bold text-ink-900">{step === 1 ? t('reports.defineParameters') : 'Configure inputs'}</h2></div><AppButton variant="ghost" size="icon-sm" className="bg-transparent text-foreground hover:bg-muted" onClick={onClose} aria-label="Close publish dialog"><X /></AppButton></header>
      <div className="min-h-0 flex-1 overflow-y-auto p-6">
        {step === 1 ? <div><p className="text-[13px] leading-relaxed text-ink-500">{t('reports.chooseFields')}</p><section className="mt-5 min-h-[420px] rounded-[14px] border border-surface-200 p-6"><div className="flex items-center justify-between"><h3 className="text-[16px] font-bold text-ink-900">{t('reports.reportParameters')} ({selected.length})</h3><AppButton type="button" variant="success-outline" size="action-md" onClick={() => setSelectorOpen(true)}>{t('reports.addParameters')}</AppButton></div>{selected.length === 0 && <div className="flex min-h-[330px] flex-col items-center justify-center text-center"><div className="mb-5 flex size-24 items-center justify-center rounded-xl border-2 border-surface-300 text-[44px] text-surface-400">☷</div><h4 className="text-[16px] font-bold text-ink-900">{t('reports.noParameters')}</h4><p className="mt-2 max-w-sm text-[13px] leading-relaxed text-ink-500">{t('reports.addFieldsHelp')}</p><AppButton type="button" variant="success" size="action-md" className="mt-6" onClick={() => setSelectorOpen(true)}>{t('reports.addParameters')}</AppButton></div>}{selectorOpen && <div className="mt-5 rounded-[14px] border border-surface-200 bg-surface-tertiary p-4"><div className="grid gap-2 sm:grid-cols-2">{mockSchema.flatMap((table) => table.columns.map((column) => { const key = `${table.name}.${column.name}`; return <label key={key} className="flex cursor-pointer items-center gap-3 rounded-[11px] border border-surface-200 bg-white p-3"><input type="checkbox" checked={selected.includes(key)} onChange={() => setSelected((current) => current.includes(key) ? current.filter((item) => item !== key) : [...current, key])} /><span className="text-[12px] font-bold text-ink-700">{column.label}</span></label>; }))}</div><AppButton type="button" variant="ghost" size="sm" onClick={() => setSelectorOpen(false)}>Done</AppButton></div>}</section></div> : <div className="flex flex-col gap-3">{parameters.map((parameter, index) => <div key={parameter.id} className="grid gap-3 rounded-[14px] border border-surface-200 p-4 md:grid-cols-[1fr_170px_auto]"><label className="text-[11px] font-bold text-ink-500">Display label<input value={parameter.label} onChange={(event) => setParameters((current) => current.map((item, itemIndex) => itemIndex === index ? { ...item, label: event.target.value } : item))} className="input mt-1.5 text-[12px]" /></label><label className="text-[11px] font-bold text-ink-500">Input type<select value={parameter.type} onChange={(event) => setParameters((current) => current.map((item, itemIndex) => itemIndex === index ? { ...item, type: event.target.value as ParameterInputType } : item))} className="input mt-1.5 text-[12px]"><option value="date">Date</option><option value="date-range">Date range</option><option value="single-select">Single select</option><option value="multi-select">Multi select</option><option value="number">Number</option><option value="text">Text</option></select></label><label className="flex items-center gap-2 self-end pb-2 text-[11px] font-bold text-ink-700"><input type="checkbox" checked={parameter.required} onChange={(event) => setParameters((current) => current.map((item, itemIndex) => itemIndex === index ? { ...item, required: event.target.checked } : item))} className="accent-emerald-500" />Required</label></div>)}</div>}
      </div>
      <footer className="flex items-center justify-between border-t border-surface-200 bg-surface-tertiary px-6 py-4"><AppButton onClick={() => step === 2 ? setStep(1) : onClose()} variant="ghost" size="action-md"><ArrowLeft />{step === 2 ? 'Back' : 'Cancel'}</AppButton>{step === 1 ? <AppButton disabled={selected.length === 0} onClick={configure} variant="primary" size="action-md">Configure {selected.length} inputs<ArrowRight /></AppButton> : <AppButton onClick={() => onPublished(parameters)} variant="primary" size="action-md"><Check />Publish report</AppButton>}</footer>
    </div>
  </div>;
}

import { useState } from 'react';
import { useT } from '@/providers/I18nProvider';
import { AppButton } from '@/components/app/AppButton';
import { AppInput } from '@/components/app/AppInput';
import { ParameterFieldsPopover } from '@/components/ParameterFieldsPopover';
import { ArrowLeft, ArrowRight, Check, Database, X } from 'lucide-react';
import type { ReportParameterField } from '@/api/types/report';
import type { ParameterInputType, ReportParameter } from '@/data/reportTemplates';

export function PublishReportDialog({ onClose, onPublished }: { onClose: () => void; onPublished: (parameters: ReportParameter[]) => void }) {
  const t = useT();
  const [step, setStep] = useState<1 | 2>(1);
  const [selectedFields, setSelectedFields] = useState<ReportParameterField[]>([]);
  const [parameters, setParameters] = useState<ReportParameter[]>([]);

  function configure() {
    setParameters(selectedFields.map((field) => ({ id: field.id, table: field.group, column: field.id, label: field.displayName, type: field.dataType === 'DATE' ? 'date-range' : field.dataType === 'NUMBER' ? 'number' : 'text', required: true, defaultValue: field.dataType === 'DATE' ? [] : '' })));

    setStep(2);
  }

  return <div className="fixed inset-0 z-[70] flex items-center justify-center bg-navy-900/55 p-4" role="dialog" aria-modal="true" aria-labelledby="publish-title">
    <div className="flex max-h-[88vh] w-full max-w-3xl flex-col overflow-hidden rounded-[20px] bg-white shadow-2xl">
      <header className="flex items-center justify-between border-b border-surface-200 px-6 py-4"><div><p className="text-[11px] font-bold uppercase tracking-[0.12em] text-mint-700">{t('reports.stepOfTwo', { step })}</p><h2 id="publish-title" className="mt-1 font-display text-[20px] font-bold text-ink-900">{step === 1 ? t('reports.defineParameters') : 'Configure inputs'}</h2></div><AppButton variant="ghost" size="icon-sm" className="bg-transparent text-foreground hover:bg-muted" onClick={onClose} aria-label="Close publish dialog"><X /></AppButton></header>
      <div className="min-h-0 flex-1 overflow-y-auto p-6">
        {step === 1 ? <div><p className="text-[13px] leading-relaxed text-ink-500">{t('reports.chooseFields')}</p><section className="mt-5 min-h-[420px] rounded-[14px] border border-surface-200 p-6"><div className="flex items-center justify-between"><h3 className="text-[16px] font-bold text-ink-900">{t('reports.reportParameters')} ({selectedFields.length})</h3><ParameterFieldsPopover reportId="draft" selectedIds={selectedFields.map((field) => field.id)} onAdd={(fields) => setSelectedFields((current) => [...current, ...fields.filter((field) => !current.some((item) => item.id === field.id))])}><AppButton type="button" variant="success-outline" size="action-md">{selectedFields.length ? t('reports.addMore') : t('reports.addParameters')}</AppButton></ParameterFieldsPopover></div>{selectedFields.length === 0 ? <div className="flex min-h-[330px] flex-col items-center justify-center text-center"><div className="mb-5 flex size-24 items-center justify-center rounded-xl border-2 border-surface-300 text-[44px] text-surface-400">☷</div><h4 className="text-[16px] font-bold text-ink-900">{t('reports.noParameters')}</h4><p className="mt-2 max-w-sm text-[13px] leading-relaxed text-ink-500">{t('reports.addFieldsHelp')}</p><ParameterFieldsPopover reportId="draft" selectedIds={selectedFields.map((field) => field.id)} onAdd={(fields) => setSelectedFields((current) => [...current, ...fields.filter((field) => !current.some((item) => item.id === field.id))])}><AppButton type="button" variant="success" size="action-md" className="mt-6">{t('reports.addParameters')}</AppButton></ParameterFieldsPopover></div> : <div className="mt-5 flex flex-col gap-3">{selectedFields.map((field) => <div key={field.id} className="flex items-center justify-between rounded-[14px] border border-surface-200 p-4"><div><p className="text-[13px] font-bold text-ink-900">{field.displayName}</p><p className="mt-1 text-[11px] text-ink-500">{field.dataType}</p></div><AppButton type="button" variant="ghost" size="icon-sm" aria-label={`Remove ${field.displayName}`} onClick={() => setSelectedFields((current) => current.filter((item) => item.id !== field.id))}>×</AppButton></div>)}</div>}</section></div> : <div className="flex flex-col gap-3">{parameters.map((parameter, index) => <div key={parameter.id} className="grid gap-3 rounded-[14px] border border-surface-200 p-4 md:grid-cols-[1fr_170px_auto]"><label className="text-[11px] font-bold text-ink-500">Display label<input value={parameter.label} onChange={(event) => setParameters((current) => current.map((item, itemIndex) => itemIndex === index ? { ...item, label: event.target.value } : item))} className="input mt-1.5 text-[12px]" /></label><label className="text-[11px] font-bold text-ink-500">Input type<select value={parameter.type} onChange={(event) => setParameters((current) => current.map((item, itemIndex) => itemIndex === index ? { ...item, type: event.target.value as ParameterInputType } : item))} className="input mt-1.5 text-[12px]"><option value="date">Date</option><option value="date-range">Date range</option><option value="single-select">Single select</option><option value="multi-select">Multi select</option><option value="number">Number</option><option value="text">Text</option></select></label><label className="flex items-center gap-2 self-end pb-2 text-[11px] font-bold text-ink-700"><input type="checkbox" checked={parameter.required} onChange={(event) => setParameters((current) => current.map((item, itemIndex) => itemIndex === index ? { ...item, required: event.target.checked } : item))} className="accent-emerald-500" />Required</label></div>)}</div>}
      </div>
      <footer className="flex items-center justify-between border-t border-surface-200 bg-surface-tertiary px-6 py-4"><AppButton onClick={() => step === 2 ? setStep(1) : onClose()} variant="ghost" size="action-md"><ArrowLeft />{step === 2 ? 'Back' : 'Cancel'}</AppButton>{step === 1 ? <AppButton disabled={selectedFields.length === 0} onClick={configure} variant="primary" size="action-md">Configure {selectedFields.length} inputs<ArrowRight /></AppButton> : <AppButton onClick={() => onPublished(parameters)} variant="primary" size="action-md"><Check />Publish report</AppButton>}</footer>
    </div>
  </div>;
}
